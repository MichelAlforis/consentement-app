# Repo PRIVÉ qui stockera les certificats + profils chiffrés
# TODO: créer un repo privé GitHub (ex: MichelAlforis/consentement-certs) et coller l'URL ici
git_url(ENV["MATCH_GIT_URL"] || "https://github.com/MichelAlforis/consentement-certs.git")

storage_mode("git")
type("appstore")

app_identifier([
  "fr.consentement.app",
  "fr.consentement.explicit",
])

username(ENV["APPLE_ID"] || "marquesmitch@gmail.com")
